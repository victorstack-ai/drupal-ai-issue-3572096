provider_openai.settings:
  type: mapping
  label: 'OpenAI Settings'
  mapping:
    api_key:
      type: string
      label: 'The API key for OpenAI via Key module'
    moderation:
      type: boolean
      label: 'Enable moderation requests before each request.'
